export const workflow = {
  name: 'Xcode IDE (mcpbridge)',
  description:
    "Proxy Xcode's built-in 'Xcode Tools' MCP service via `xcrun mcpbridge`. Registers dynamic `xcode_tools_*` tools when available. Bridge debug tools are only registered when `debug: true`.",
};
