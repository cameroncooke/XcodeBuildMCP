import {
  resetSimulatorLocationToolName,
  resetSimulatorLocationToolDescription,
  resetSimulatorLocationToolSchema,
  resetSimulatorLocationToolHandler,
} from '../../src/tools/simulator/index.js';

export default {
  name: resetSimulatorLocationToolName,
  description: resetSimulatorLocationToolDescription,
  schema: resetSimulatorLocationToolSchema,
  handler: resetSimulatorLocationToolHandler,
};