import { keyPressToolName, keyPressToolDescription, keyPressToolSchema, keyPressToolHandler } from '../../src/tools/axe/index.js';

export default {
  name: keyPressToolName,
  description: keyPressToolDescription,
  schema: keyPressToolSchema,
  handler: keyPressToolHandler,
};